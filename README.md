# PR screenshot assets

Images referenced from pull request descriptions. This branch is **not** part of
the project — it shares no history with `main` and must never be merged.

It exists because images cannot be attached to a PR body from the CLI, and
screenshots do not belong in `main`: this repository produces design systems and
contains no products.

Delete a directory once its PR is closed and the description no longer needs to
render.
